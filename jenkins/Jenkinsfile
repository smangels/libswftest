node { // <1>
	checkout scm // everything starts with beautiful source code

    stage('Build') { // <2>
		sh 'mkdir build && cd build && cmake .. && cmake --build .'
    }
    stage('Test') {
        /* .. snip .. */
    }
    stage('Deploy') {
        /* .. snip .. */
    }
}